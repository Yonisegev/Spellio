import { KeyboardClickDirective } from './keyboard-click.directive';

describe('KeyboardClickDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyboardClickDirective();
    expect(directive).toBeTruthy();
  });
});
